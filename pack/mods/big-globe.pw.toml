name = "Big Globe (shallow-464 patched)"
filename = "bigglobe-5.3.2-mc1.21.1-shallow464.jar"
side = "both"

# Height-patched Big Globe 5.3.2: overworld floor -1024 -> -464, ceiling +896, deep tiers
# compacted. BG reads its generator from its own jar every load (reload_dimension), so the
# height change MUST live inside the jar — datapacks/companion mods can't override it.
# Built from the official 5.3.2 jar via bigGlobeAero/build_patched_jar.py (5 files swapped).
# Big Globe by builderb0y (CC BY-NC 4.0); modified for personal-server use, not the vanilla jar.
# No [update] block on purpose: prevents `packwiz update` reverting to the unpatched Modrinth jar.

[download]
url = "https://raw.githubusercontent.com/Minecraft-Bonanaza/BraveNewGlobe/main/bundled-jars/bigglobe-5.3.2-mc1.21.1-shallow464.jar"
hash-format = "sha256"
hash = "bcee9a49e0a904d79244942cf2c0ac2598edcb20396138058dd08e1aea60cec8"
